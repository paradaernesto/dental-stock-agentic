# Health Check

Run system health check.

## Run

```bash
# Check Node version
node --version

# Check pnpm
pnpm --version

# Check dependencies
pnpm list --depth=0

# Run tests
pnpm test
```

## Report

Health status of the system.
